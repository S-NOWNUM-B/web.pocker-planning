// cn — это утилита для объединения классов CSS. Она принимает произвольное количество аргументов, которые могут быть строками, undefined, false или null. Функция фильтрует эти аргументы, удаляя все falsy значения (undefined, false, null) и объединяет оставшиеся строки в одну строку с классами, разделенными пробелами. Это позволяет легко управлять динамическими классами в компонентах UI, обеспечивая чистый и удобочитаемый код при условной логике для классов CSS.
export function cn(...classes: (string | undefined | false | null)[]): string {
  return classes.filter(Boolean).join(' ');
}
